# Gravity Dots

A small interactive particle field made with HTML, CSS and JavaScript.

Move the pointer to attract dots. Click to scatter them. Use Reset field to generate a fresh layout.

## Run

Open index.html in a modern browser. There is no install or build step and no external dependency.

## Screenshot tip

Open the page at desktop width, move the pointer near the center to form a cluster, and capture the browser viewport.