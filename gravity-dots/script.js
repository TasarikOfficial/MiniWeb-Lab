const c=document.querySelector('#field'),x=c.getContext('2d');let dots=[],mouse={x:0,y:0,on:false};function size(){const r=c.getBoundingClientRect(),d=devicePixelRatio||1;c.width=r.width*d;c.height=r.height*d;x.setTransform(d,0,0,d,0,0);seed()}function seed(){const r=c.getBoundingClientRect();dots=Array.from({length:70},()=>({x:Math.random()*r.width,y:Math.random()*r.height,vx:0,vy:0,r:1.5+Math.random()*3}))}function point(e){const r=c.getBoundingClientRect(),p=e.touches?e.touches[0]:e;mouse.x=p.clientX-r.left;mouse.y=p.clientY-r.top;mouse.on=true}c.addEventListener('pointermove',point);c.addEventListener('pointerleave',()=>mouse.on=false);c.addEventListener('pointerdown',e=>{point(e);dots.forEach(d=>{let a=Math.atan2(d.y-mouse.y,d.x-mouse.x),q=2+Math.random()*5;d.vx+=Math.cos(a)*q;d.vy+=Math.sin(a)*q})});document.querySelector('#reset').onclick=seed;function draw(){const r=c.getBoundingClientRect();x.clearRect(0,0,r.width,r.height);for(const d of dots){if(mouse.on){let dx=mouse.x-d.x,dy=mouse.y-d.y,z=dx*dx+dy*dy;if(z<50000&&z>100){d.vx+=dx/z*5;d.vy+=dy/z*5}}d.vx*=.97;d.vy*=.97;d.x+=d.vx;d.y+=d.vy;if(d.x<0||d.x>r.width)d.vx*=-1;if(d.y<0||d.y>r.height)d.vy*=-1;d.x=Math.max(0,Math.min(r.width,d.x));d.y=Math.max(0,Math.min(r.height,d.y));x.beginPath();x.arc(d.x,d.y,d.r,0,Math.PI*2);x.fillStyle='#171717';x.fill()}requestAnimationFrame(draw)}addEventListener('resize',size);size();draw();